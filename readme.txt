You can start the project by using the following three commands

"catkin_make"
"source devel/setup.bash"
"roslaunch box_detector box_detector.launch"

And the detected plane (named plane_*.pcd) is stored in the folder "/home/username/.ros/"

